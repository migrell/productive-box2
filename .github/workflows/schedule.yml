name: Update gist
on:
  push:
    branches:
      - master
  schedule:
   - cron: "*/10 * * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: migrell/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: 7d82b814f09fc2ec5c90384ec36552e5
          TIMEZONE: Asia/Seoul
